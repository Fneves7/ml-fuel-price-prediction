# Dependências do projeto (versões fixadas — C3, para reprodutibilidade).
# Instalar com: pip install -r requirements.txt

# --- núcleo (exigido pelo enunciado) ---
pandas==3.0.3
numpy==2.3.5
scikit-learn==1.9.0
matplotlib==3.11.0
seaborn==0.13.2

# --- modelação avançada ---
xgboost==3.4.1
lightgbm==4.7.0
catboost==1.2.10
shap==0.52.0            # B11 — explicabilidade
joblib==1.5.3           # F2/C5 — guardar modelos

# --- séries temporais (scripts/supervisionada/03) ---
statsmodels==0.14.6     # ARIMA, SARIMA, suavização exponencial
prophet==1.4.0
tensorflow==2.21.0      # LSTM

# --- opcional: extrair Portarias do ISP dos PDFs do Diário da República ---
pypdf==6.16.2
cryptography==50.0.1    # os PDFs do DR estão cifrados (AES)
