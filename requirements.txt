# Dependências do projeto (instalar com: pip install -r requirements.txt)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
xgboost>=2.0      # B3 / métodos de conjunto
lightgbm>=4.0     # B3 — gradient boosting
# --- família completa de algoritmos supervisionados (scripts/supervisionada/) ---
statsmodels>=0.14 # séries temporais: ARIMA, SARIMA, suavização exponencial
catboost>=1.2     # métodos de conjunto
prophet>=1.1      # séries temporais
tensorflow>=2.15  # séries temporais: LSTM
shap>=0.44        # B11 — explicabilidade
joblib>=1.3       # F2/C5 — guardar modelos (já vem com scikit-learn)
pypdf>=4.0        # opcional: extrair Portarias de ISP dos PDFs do DR
cryptography>=3.1 # opcional: PDFs do DR estão cifrados (AES)
